<resources>
    <!-- Drawing into the notch is API 27 and up; minSdk is 26, where the attribute would be a lint error
         for no gain (the platform ignores it). -->
    <style name="Theme.OneEmu.Emulator" parent="Theme.OneEmu">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
